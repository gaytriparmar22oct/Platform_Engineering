# dev overrides for catalog
image:
  repository: acridpdev.azurecr.io/catalog   # ACR set later by Terragrunt
  tag: "0.1.0"                                # Kargo will bump this on promotion

replicaCount: 2
autoscaling:
  enabled: false

resources:
  requests: { cpu: 100m, memory: 128Mi }
  limits:   { cpu: 500m, memory: 256Mi }
