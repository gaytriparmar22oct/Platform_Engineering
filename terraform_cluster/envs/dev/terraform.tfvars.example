name                = "idp-dev"
location            = "eastus"
resource_group_name = "rg-idp-dev"

# Optional: grant cluster-admin to an Entra ID group
# admin_group_object_ids = ["00000000-0000-0000-0000-000000000000"]

# Optional: lock down the API server (recommended after first apply)
# authorized_ip_ranges = ["203.0.113.4/32"]

tags = {
  environment = "dev"
  owner       = "platform-team"
  costcenter  = "platform"
}
